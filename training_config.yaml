training:
  epochs: 10
  batch_size: 32
  learning_rate: 0.001
  optimizer: adam
  scheduler:
    type: cosine
    warmup_steps: 100
model:
  name: bert-base-uncased
  num_labels: 3
  dropout: 0.1
data:
  train_path: data/train.csv
  val_path: data/val.csv
  max_length: 512
